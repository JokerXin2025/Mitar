# Mitar 证明架构规范

## 概略 Strategy

概略是指由一些特定的策略组合而成的解题框架，其在宏观上确定了整个证明的思路和结构, Mitar 会使用不同颜色的大型色块以区分不同的概略.

| 概略 | 引导策略 | 重要节点 | 颜色 |
| :- | :- | :- | :- |
| 反证法 | `by_contra` / `intro` (目标为 `False` 时) | | 红色 |
| 分类讨论 | `by_cases` / `cases` | `·` | 无色 |
| 目标拆解 | `constructor` | `·` | 无色 |
| 数学归纳法 | `induction with` | <code>&#124; zero =></code> , <code>&#124; succ k ih =></code> | 绿色 |

上表中通配符 `·` 也可以替换成实际的构造子名称

## 策略 Tactics

策略是证明脚本中的基本步骤单元, 通常来说, 知识宇宙会提供一些定制策略作为优选策略, 本节对 Lean4 with Mathlib 的原生策略进行分类归纳.

### 沉默策略

这些策略可能使 Lean 证明状态发生改变, 但在证明逻辑上未作出本质推进, 因而可以不受限制地使用, 但 Mitar 的翻译系统会自动跳过它们.

| 沉默策略 |
| :- |
| `intro` / `intros` |
| `revert` |
| `clear` |
| `rename` |

### 禁用策略

这些策略将被禁止在 Mitar 生成的证明中出现, 如果知识宇宙中某些内部定理的证明是在 Mitar 中可见的, 则它们同样被禁止使用这些策略.

| 禁用策略 | 修正建议 |
| :- | :- |
| `focus` , `{}` | 请使用 `·` |
| `done` | 在证明中使用无意义 |
| `assumption` | 请明确使用的假设, 并使用 `exact` |
| `rintro` | 请使用 `intro` |
| `aesop` | 请使用 `aesop?` 获取详细的证明脚本 |
| `admit` | 请暂时使用 `sorry` |
| `sorry` | 仅在知识宇宙开发阶段使用 |

### 优选策略

| 优选策略 | 模板 |
| :- | :- |
| `rfl` | 该命题显然成立 |
| `trivial` | 该命题显然成立 |
| `apply` | 从已知条件...可推出... / 要证明..., 只需证明...即可 |
| `unfold` | 根据...的定义, 我们有... / 根据...的定义, 我们需要证明... |
| `change` | 条件...又可写作... / 原命题等价于... |
| `gcongr` | 要证明..., 只需证明...即可 / 不等式...可通过对已知条件进行保序变换得到 |

### 可选策略

| 可选策略 | 模板 |
| :- | :- |
| `omega` | 通过算术推导即可证明... / 导出算术矛盾 |
| `ring` | 通过代数推导即可证明... |
| `linarith` | 通过不等式推导即可证明... / 导出不等式矛盾 |
| `nlinarith` | 通过不等式推导即可证明... / 导出不等式矛盾 |
| `norm_num` | 通过计算可得... / 目标可标准化为... / 通过数值计算即可证明... / 算式...不成立 / 通过数值计算获得矛盾 |

### 限制

- 关于 `exact` , 详见章节 __项 & `exact`__ .
- 请务必使用 `simp only` 代替 `simp` , 可以暂时使用 `simp?` 来查看哪些化简引理会被使用.

## 项 & `exact`

## 子目标 `have`

## 计算块 `calc`